These testcases are all generated by scripts.
You can copy them, run the program and paste them in command line to test them again.

The file name with maxval means that it's generated for maxval test, so as k. And the file without any other parameter name means the it's generated for n test.